###prop
shouldFail=true
selectionType=points
startLine=8
startColumn=21
endLine=8
endColumn=30
deleteMethod=true
allInvocations=true
onlySelectedInvocation=false
###src
/*
 * Test11 Impossible Inline !!
 * only methods with simple 1 return line can be inlined
 * in a call chain
 */
class InlineSamples11{
    static void main(args) {
        InlineSamples11 sp = new InlineSamples11()
        def res = sp.extract().substring(2)
        println(res)
    }
    String extract() {
        String s = new String("TEST")
        return s.toLowerCase()
    }
}
###exp
/*
 * Test11 Impossible Inline !!
 * only methods with simple 1 return line can be inlined
 * in a call chain
 */
class InlineSamples11{
    static void main(args) {
        InlineSamples11 sp = new InlineSamples11()
        def res = sp.extract().substring(2)
        println(res)
    }
    String extract() {
        String s = new String("TEST")
        return s.toLowerCase()
    }
}
###end